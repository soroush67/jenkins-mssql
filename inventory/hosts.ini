# Placeholder inventory - deploys to this same machine via
# ansible_connection=local, matching this user's other Ansible+Jenkins
# projects (gitlab-backup-with-jenkins-ansible, CockroachDB-Cluster,
# jenkins-postgresql). Real deployments don't need to touch this at
# all - pass -e target_host=<ip> (Jenkins: TARGET_HOST) instead; see
# playbooks/deploy.yml's own comments for why.

[mssql]
localhost ansible_connection=local
